<?xml version="1.0" encoding="utf-8"?>
<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Larger blue circle in back -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@color/primary_color"/>
            <size
                android:width="15dp"
                android:height="15dp"/>
        </shape>
    </item>
    <!-- Smaller red circle in front -->
    <item>
        <shape android:shape="oval">
            <!-- transparent stroke = larger_circle_size - smaller_circle_size -->
            <stroke android:color="@android:color/transparent"
                android:width="1dp"/>
            <solid android:color="@color/secondary_color"/>
            <size
                android:width="10dp"
                android:height="10dp"/>
        </shape>
    </item>
    <item>
        <shape android:shape="oval">
            <!-- transparent stroke = larger_circle_size - smaller_circle_size -->
            <stroke android:color="@android:color/transparent"
                android:width="4dp"/>
            <solid android:color="@color/primary_color"/>
            <size
                android:width="10dp"
                android:height="10dp"/>
        </shape>
    </item>
    <item>
        <shape android:shape="oval">
            <!-- transparent stroke = larger_circle_size - smaller_circle_size -->
            <stroke android:color="@android:color/transparent"
                android:width="5dp"/>
            <solid android:color="@color/secondary_color"/>
            <size
                android:width="10dp"
                android:height="10dp"/>
        </shape>
    </item>
    <item android:drawable="@drawable/ic_near_me_yellow_24dp"
        android:top="@dimen/margin_5"
        android:bottom="@dimen/margin_5"
        android:left="@dimen/margin_5"
        android:right="@dimen/margin_5">
    </item>
</layer-list>